ekf_node:
  ros__parameters:
    odom_frame: odom
    base_link_frame: base_link
    world_frame: odom

    two_d_mode: true

    frequency: 20.0

    # odom0: husky_velocity_controller/odom
    #odom0: /odom
    odom0: /amiga_velocity_controller/odom
    odom0_config: [true, true, false,
                    false, false, false,
                    false, false, false,
                    false, false, false,
                    false, false, false]
    # odom0_differential: false
    odom0_differential: true
    odom0_queue_size: 10

    imu0: /imu/data_raw
    imu0_config: [false, false, false,
                  false, false, true,
                  true, false, false,
                  false, false, true,
                  true, true, false]
    imu0_differential: false
    imu0_queue_size: 10
    imu0_remove_gravitational_acceleration: true

    # buffer_timeout: 0.2
